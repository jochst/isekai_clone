# StorylinePage Specification (/storylines/[id])

## Overview
- **Target files:**
  - `src/components/sites/isekaizero-ai-0e4f18da/storyline/StorylinePage.tsx` (client; page body rendered inside `AppShell nav="topbar"`)
  - `src/components/sites/isekaizero-ai-0e4f18da/storyline/CoverCard.tsx`
  - `src/components/sites/isekaizero-ai-0e4f18da/storyline/CastsGrid.tsx`
  - `src/components/sites/isekaizero-ai-0e4f18da/storyline/StoryInfo.tsx` (title block + tags + stats + reactions + creator banner)
  - `src/components/sites/isekaizero-ai-0e4f18da/storyline/DetailSections.tsx` (Detailed Information / Token Info / Related Content accordions)
  - `src/components/sites/isekaizero-ai-0e4f18da/storyline/CommentsPanel.tsx`
  - `src/components/sites/isekaizero-ai-0e4f18da/storyline/StartBar.tsx` (sticky bottom bar + "How do you want to play?" modal)
- **Screenshots:** `storyline-desktop-1440.png`, `_tmp_story_desktop.png`, `storyline-mobile-390.png`, `_tmp_after_start.png` (play modal).
- **Interaction model:** click-driven. Accordions toggle; tag row expands with the chevron; "Start Now" opens the play modal; "Play as Guest" creates a chat session and routes to `/chats/[newId]`.

## Layout (desktop 1440; content column x=192..1248 → `max-width:1056px; margin:0 auto; padding:20px 0 120px`)
- Two columns `display:flex; gap:16px; align-items:flex-start`: left `width:400px; flex:0 1 400px` (CoverCard + CommentsPanel), right `flex:1` (Casts, StoryInfo, DetailSections).
- <1024px (see mobile screenshot): single column; cover card first (full width, no border/shadow, `border-radius:0`), then title block, then casts strip (horizontal scroll), tags, stats, creator, details, comments. StartBar stays fixed at the bottom.

## CoverCard (exact)
- Card `width:400px; border-radius:16px; background: rgba(11,15,42,0.6); border:1px solid rgba(255,255,255,0.1); box-shadow: 0 8px 20px rgba(0,0,0,0.3); overflow:hidden`.
- Media `aspect-ratio: 3/4; width:100%; background: rgba(200,200,220,0.15); position:relative` containing `<StoryCover mode="portrait">` (from `shared/StoryCover.tsx`).
  - Top overlay row `position:absolute; top:12px; left:12px; right:12px; z-index:10; flex row; justify-content:space-between`: back button `38×38; border-radius:20px; background: rgba(0,0,0,0.3); flex center` lucide `ChevronLeft` 22px (→ `router.back()`), more button same chrome with lucide `MoreVertical` 22px.
  - Next-image button `position:absolute; right:12px; bottom:12px; 38×38` same chrome with lucide `ChevronRight` 22px (cycles between the cover and character portraits).
- Footer `padding:12px; flex col; gap:8px`: row `margin-bottom:6px; flex row; justify-content:space-between; align-items:center`:
  - Left group `flex row; flex-wrap:wrap; gap:6px; align-items:center`: "Public" chip `height:24px; padding:4px 8px; border-radius:6px; background: rgba(255,255,255,0.2); flex row; gap:4px` with lucide `TrendingUp` 14px `rgb(76,175,80)` + "Public" `font-size:12px; font-weight:600`; then 22×22 icon chips `padding:4px; border-radius:6px` (14px icons): SFW shield `background: rgba(52,152,219,0.8)` (lucide `ShieldCheck`), Monetized `rgba(34,197,94,0.8)` (`DollarSign`), Featured `rgb(218,165,32)` (`Star`), Visual Novel `rgba(138,43,226,0.8)` (`BookOpen`). Render per flags (sfw/monetized/featured/visualNovelReady).
  - Right: gallery chip `height:27px; padding:4px 8px; border-radius:6px; background: rgba(255,255,255,0.2); flex row; gap:4px` lucide `Images` 18px + count `font-size:14px; font-weight:600` (= characters.length + 1).

## CommentsPanel (left column, below cover, `margin-top:16px`)
- Header row `flex row; justify-content:space-between; align-items:center; padding:0 12px`: "{n} Comments" `font-family: Roboto; font-weight:600; font-size:16px` with gradient text `linear-gradient(to right, rgb(255,255,255), rgb(255,191,191))`; sort button `height:34px; padding:6px 10px; border-radius:8px; background: rgba(255,255,255,0.06); border:1px solid rgba(255,255,255,0.1); flex row; gap:4px; align-items:center` lucide `Flame` 18px `rgb(184,192,216)` + "Best" `font-size:14px; color: rgb(184,192,216)` + `ChevronDown` 20px.
- Composer `margin:16px 12px 0; height:48px; border-radius:12px; background: rgba(255,255,255,0.04); border:1px solid rgba(255,255,255,0.12); padding:0 14px; flex row; gap:10px; align-items:center`: lucide `MessageCircle` 18px `rgba(255,255,255,0.5)` + placeholder "Join the conversation" `font-size:14px; color: rgba(255,255,255,0.4)`.
- Comment items (mock, generate 5 from a small local list of original friendly comments keyed by storyline id): `padding:14px 12px 0; flex row; gap:10px`: avatar `36×36; border-radius:18px` gradient (creator-style); body: handle `font-size:12px; font-weight:600; color: rgb(255,168,168)` + date `font-size:11px; color: rgba(255,255,255,0.45); margin-left:6px`; text `font-size:14px; line-height:19px; margin-top:4px`; actions row `margin-top:6px; flex row; gap:14px; font-size:12px; color: rgba(255,255,255,0.6)` with lucide `ThumbsUp` 14 + count, `ThumbsDown` 14 + "0", `Reply` 14, `Flag` 14.
- "Load more comments" button `margin:16px auto 0; height:34px; padding:0 16px; border-radius:17px; background: rgba(255,255,255,0.08); font-size:13px`.

## CastsGrid (right column top)
- Header `flex row; align-items:center; gap:10px; margin-bottom:12px`: 3px×20px bar `background: rgb(167,185,255); border-radius:2px` (visible in screenshot left of "Casts"), "Casts" `font-size:18px; font-weight:700; letter-spacing:0.3px; color: rgb(240,245,255)`, count pill `padding:2px 8px; border-radius:10px; background: rgba(255,255,255,0.08); font-size:13px; font-weight:500; color: rgb(184,192,216)`.
- Grid `display:flex; flex-wrap:wrap; gap:10px`: cast tile `<button> width:94px; height:122px; border-radius:10px; border:2px solid rgba(167,185,255,0.45); background: rgba(255,255,255,0.05); overflow:hidden; position:relative` containing `<CharacterPortrait>` full-bleed + name label at bottom `position:absolute; left:0; right:0; bottom:6px; padding:0 4px; font-size:12px; font-weight:600; text-align:center; text-shadow: 0 1px 3px rgba(0,0,0,0.8); line-height:14px` (2 lines max). Clicking selects the character and shows it in the CoverCard media (via shared state `selectedIndex`).
- <1024px: horizontal strip `overflow-x:auto; gap:10px` with the same tiles.

## StoryInfo
- Title box `margin-top:16px; padding:12px; border-radius:12px; background: rgba(255,255,255,0.03); border:1px solid rgba(255,168,168,0.35); box-shadow: 0 0 0 1px rgba(167,185,255,0.08)` (screenshot shows a soft pink-outlined panel): title `font-size:28px; font-weight:700; color: rgb(240,245,255); margin-bottom:10px; text-align:left`; tagline `font-size:14px; line-height:19px; color: rgb(240,245,255)`; meta `margin-top:8px; font-size:12px; color: rgb(138,146,176); opacity:0.7` "Published {d MMM yyyy} | Updated {d MMM yyyy} (v{version})".
- Tags row `margin-top:14px; flex row; gap:8px; align-items:center; overflow:hidden; height:26px` (expands to wrap when the chevron is clicked): expand button `34×26; border-radius:13px; background: rgba(255,255,255,0.12); flex center` lucide `ChevronDown` 16px (rotate 180° when expanded); category chip `height:26px; padding:3px 12px; border-radius:13px; background: rgba(255,255,255,0.22); flex row; gap:6px` 14px category icon + label `font-size:12px; font-weight:600`; tag chips `height:26px; padding:3px 12px; border-radius:13px; background: rgba(255,255,255,0.1); font-size:12px; font-weight:600; color: rgba(255,255,255,0.85)` text "#{tag}".
- Stats bar `margin-top:14px; height:52px; border-top:1px solid rgba(255,255,255,0.08); border-bottom:1px solid rgba(255,255,255,0.08); display:flex; justify-content:space-around; align-items:center`: each item `flex row; align-items:center` icon 18–20px + value `font-size:13px; font-weight:500; margin-left:4px`: plays (lucide `MessageCircle` 18), chats (`MessagesSquare` 18), likes (`Heart` 20), saves (`Bookmark` 20), comments (`MessageSquareText` 20), gifts pill (`height:28px; padding:0 10px; border-radius:14px; background: rgba(255,168,168,0.35)` lucide `Gift` 16 + count `font-size:14px; font-weight:700`). Values use `formatCountLower` ("410k", "3k", "1k").
- Reactions row `margin-top:8px; height:40px; display:flex; justify-content:space-around; align-items:center`: four emoji reactions (🌹 🏆 🧪 💖) each `font-size:22px` + count `font-size:10px; font-weight:600; color: rgba(255,255,255,0.6); margin-left:4px`.
- Creator banner `margin-top:16px; height:60px; border-radius:12px; background: linear-gradient(90deg, rgba(167,185,255,0.18), rgba(255,168,168,0.18)); border:1px solid rgba(255,255,255,0.12); padding:8px 12px; flex row; align-items:center; gap:10px; position:relative; overflow:hidden`: avatar `44×44; border-radius:22px` (creator gradient with initials `font-weight:700`); text col `flex:1`: row handle `font-size:14px; font-weight:700` + lucide `BadgeCheck` 16px `rgb(74,158,255)` + "{followers} followers" `font-size:12px; color: rgba(255,255,255,0.5)`; bio `margin-top:2px; font-size:12px; color: rgba(255,255,255,0.5); white-space:nowrap; overflow:hidden; text-overflow:ellipsis`; Follow button `height:36px; padding:0 20px; border-radius:18px; background: rgb(42,65,228); font-size:13px; font-weight:600` (toggles to "Following" with `background: rgba(255,255,255,0.15)`).

## DetailSections (accordions)
- Section header `margin-top:20px; height:26px; flex row; justify-content:space-between; align-items:center`: title `font-family: Roboto; font-weight:600; font-size:16px` gradient text `linear-gradient(to right, rgb(255,255,255), rgb(255,152,152))` ("Detailed Information", "Token Info", "Related Content"); lucide `ChevronUp` 22px `rgb(240,245,255)` (rotates when collapsed).
- Detailed Information body: tab row `margin-top:12px; flex row; gap:24px; border-bottom:1px solid rgba(255,255,255,0.08)`: tabs "Plot" / "Scenarios" `padding:6px 0 10px; font-size:14px; font-weight:600; color: rgba(255,255,255,0.6)`; active `color:#fff` with underline `height:2px; background: rgb(255,168,168)`. Panel `margin-top:14px; padding:16px; border-radius:12px; background: rgba(0,0,0,0.25); border:1px solid rgba(255,255,255,0.08); font-size:14px; line-height:22px; color: rgba(255,255,255,0.85); white-space:pre-wrap` showing `plot` (Plot tab) or the scenarios list (each: title `font-weight:700; margin-bottom:4px` + summary; separated by `margin-top:14px`). Below the panel two feature rows `margin-top:12px; padding:12px; border-radius:12px; background: rgba(255,255,255,0.04); flex row; gap:12px; align-items:center` with lucide icons 22px `rgb(167,185,255)` (`Swords`, `FlaskConical`): "Advanced Mode" / "There's more depth than meets the eye!" and "Secret Sauces" / "Some secrets are meant to be discovered..." (title `font-size:14px; font-weight:700`, sub `font-size:12px; color: rgba(255,255,255,0.6)`). Render only when `flags.dungeonMind`.
- Token Info body (`margin-top:12px`): rows `height:27px; flex row; justify-content:space-between` label `font-size:14px; color: rgb(148,148,148)` / value `font-size:14px`: "Storyline Tokens" {tokens.storyline as "3k"}, "Characters Tokens", then "Estimated Total" (both `font-weight:700`, `margin-top:9px`), then `margin-top:13px` "Scenario #1 Tokens" {tokens.scenario}.
- Related Content body: 3 related storylines (same category, other ids) as horizontal StoryCards (`<StoryCard variant="row">` from `shared/StoryCard.tsx`) in a `flex row; gap:8px; overflow-x:auto; margin-top:12px`.

## StartBar (sticky bottom)
- `position:sticky; bottom:12px; margin-top:24px; z-index:30; padding:10px; border-radius:16px; background: rgba(2,9,32,0.85); backdrop-filter: blur(10px); border:1px solid rgba(255,255,255,0.08); display:flex; gap:10px; align-items:center; width:100%` (mobile: `position:fixed; left:0; right:0; bottom:0; border-radius:0; padding:12px 15px`).
- Two icon buttons `48×48; border-radius:12px; background: rgba(255,255,255,0.08); border:1px solid rgba(167,185,255,0.35); flex center` lucide `Bookmark` 22px, `Heart` 22px (toggle filled pink `rgb(255,168,168)`).
- Start button `flex:1; height:48px; border-radius:12px; border:1.5px solid rgba(255,168,168,0.7); background-image: linear-gradient(90deg, rgba(255,168,168,0.25), rgba(42,65,228,0.35) 50%, rgba(167,185,255,0.25)); font-size:16px; font-weight:600; flex center` "Start Now".
- Play modal (`_tmp_after_start.png`): overlay `rgba(0,0,0,0.6)`; dialog `width:720px; max-width:calc(100% - 32px); padding:24px 20px; border-radius:16px; background: linear-gradient(rgb(17,18,60), rgb(9,25,77)); border:1.5px solid rgba(255,168,168,0.7); box-shadow: 0 20px 60px rgba(0,0,0,0.5)`: title "How do you want to play?" `font-size:18px; font-weight:700; text-align:center`; close `X` at top-right (`22px`, `rgba(255,255,255,0.7)`); text "Jump in instantly as a guest, or create an account to keep your stories and progress on all your devices." `margin-top:12px; font-size:14px; line-height:20px; text-align:center; color: rgba(255,255,255,0.85)`; buttons stacked `gap:12px; margin-top:20px`: "Play as Guest" `height:48px; border-radius:24px; border:1.5px solid rgba(255,168,168,0.8); background: linear-gradient(90deg, rgb(23,36,126), rgb(42,65,228)); font-size:15px; font-weight:600`; "Sign Up / Log In" `height:48px; border-radius:24px; border:1.5px solid rgba(255,168,168,0.8); background: rgba(255,255,255,0.05); font-size:15px; font-weight:600` (no-op → shows a small toast "Accounts are not part of this demo").
- "Play as Guest" → `createChatSession(storyline)` from `src/lib/sites/isekaizero-ai-0e4f18da/chat-store.ts` (this module is being written by the chat builder; if it does not exist in your worktree, create a stub exporting `createChatSession(storyline: Storyline): ChatSession` that builds a `ChatSession` (types in `src/types/isekaizero.ts`) with id = `crypto.randomUUID()`, the storyline's `opening` as the first assistant message, default settings, and persists it under localStorage key `iz.chats.v1` as `Record<string, ChatSession>`), then `router.push(`/chats/${session.id}`)`.

## Props
```ts
export function StorylinePage({ storyline, related }: { storyline: Storyline; related: Storyline[] })
```

## Text (verbatim)
Public · Casts · Published … | Updated … (v…) · Follow · Detailed Information · Plot · Scenarios · Advanced Mode · There's more depth than meets the eye! · Secret Sauces · Some secrets are meant to be discovered... · Token Info · Storyline Tokens · Characters Tokens · Estimated Total · Scenario #1 Tokens · Related Content · Comments · Best · Join the conversation · Load more comments · Start Now · How do you want to play? · Jump in instantly as a guest, or create an account to keep your stories and progress on all your devices. · Play as Guest · Sign Up / Log In
